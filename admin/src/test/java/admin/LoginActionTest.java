package admin;

import org.junit.Test;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.RequestBuilder;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;

import com.warehouse.web.action.LoginAction;

/**
 * 好多省略的东西
 * @author zhangsh
 *
 */
public class LoginActionTest {
	@Test
	public void testLogin(){
		/*LoginAction la = new LoginAction();
		MockMvc mockMvc = MockMvcBuilders.standaloneSetup(la).build();
		RequestBuilder requestBuilder = RequestBuilder.class.newInstance();
		mockMvc.perform(requestBuilder).andExpect(matcher)*/
	}
}
